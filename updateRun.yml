resources:
  - name: runTracker
    type: PropertyBag
    configuration:
      Hello: World
pipelines:
  - name: resource_update
    steps:
      - name: trigger_2
        type: Bash
        execution:
          onExecute:
            - write_output runTracker "pipelines=$run_number"
